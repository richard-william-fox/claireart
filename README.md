# claireart
A site to display and allow purchase of Claire Fox's art. Production url is https://clairefoxcreations.org. Use 'npm install' after cloning repo and run src/index.js. The server will require a mongodb connection string to be passed as an environment variable.
